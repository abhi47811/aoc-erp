// Shared form field styling. Was copy-pasted as an identical local
// `const inputClass = '...'` in 5+ page files -- single source now.
export const inputClass =
  'w-full bg-white text-slate-900 px-3.5 py-2.5 rounded-lg text-sm border border-slate-200 focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500 placeholder:text-slate-500 transition-colors'

export const labelClass = 'text-xs font-medium text-slate-500 uppercase tracking-wider block mb-1.5'
